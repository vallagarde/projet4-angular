stages:
  - deploy

pages:
  image: node:12-alpine
  stage: deploy
  script:
    - yarn install
    - yarn ng build
    - mkdir .public
    - cp dist/pecadille .public
    - mv .public public
  artifacts:
    paths:
      - public
  
  
